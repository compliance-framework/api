lisa-selfreview:
Pass 1 (strict). Build/vet/unit + admin & evidence integration tests all green.
- [medium][test] authz.Builtin.evaluateAdmin holds the relocated admin-group logic, but only its DENY path (missing required groups → 403) is exercised by the existing TestWorkflowImportRouteRequiresAdminGroup. The allow/edge paths (password=super-admin, SSO disabled, no required groups, SSO user WITH required group, user-not-found) have no direct test. Fix: add a sqlite-backed integration test of the admin decision matrix to lock in zero-behavior-change for the moved logic. -> ADDRESSING THIS PASS.
- [info][intentional, do-not-fix] RequireAdminGroups hard-wires the builtin driver while evidence uses the config-selected driver via authz.Open. Identical in Phase 1 (builtin is the only/default driver); RequireAdminGroups can't return an error to honor an arbitrary configured driver. Documented in lisa-design.
- [info][intentional] Deny responses are generic 403 (reason logged, not echoed) per design; only status is asserted by tests. Evidence read routes remain public (unchanged); only the create route (where evidence_auth.go's middleware lived) gains Authorize.

lisa-addressed:
Pass 1 fix: added internal/authz/builtin_admin_integration_test.go (//go:build integration, sqlite in-memory) covering the full admin decision matrix of the relocated authz.Builtin.evaluateAdmin: password=super-admin->allow, sso disabled->allow, no required groups->allow, sso user with required group->allow, sso user missing group->deny, no sso link->deny, unknown provider->deny, unknown user->deny. All 8 pass. Locks in zero-behavior-change for the logic moved out of RequireAdminGroups.
