lisa-addressed:
---
Comment (internal/service/worker/due_soon_checker.go:125):
DueSoonCheckerWorker sets SourceJobID from the checker job (workflow_due_soon_checker), but buildWorkflowTaskDueSoonNotificationRequest sets Options.SourceJobKind to "workflow_task_due_soon". That makes SourceJobKind/SourceJobID refer to different River jobs, which will confuse troubleshooting lookups and correlation. Consider overriding the request’s SourceJobKind (and ideally the correlation ID prefix) to match the checker job kind when dispatching from this worker, or introduce a helper to set both kind+ID consistently.
---
Comment (internal/service/notificationtroubleshooting/service.go:1433):
recentProviderJobs widens the provider-job selection with an OR on created_at >= latestSource.CreatedAt. This can pull in unrelated provider jobs from other notification families created after the latest source job, which can skew downstream diagnostics (status, stale/failed counts, and correlation coverage). Now that SourceJobID propagation exists, consider tightening this query to only include jobs whose source_job_kind matches the relevant kinds (and, when available, whose source_job_id matches the chosen source job), and only fall back to the created_at filter for jobs missing source_job_kind metadata.

Implementation guidance: In internal/service/worker/due_soon_checker.go, ensure provider jobs dispatched by DueSoonCheckerWorker use source metadata that consistently points to the checker River job. Do not leave SourceJobKind as worker.JobTypeWorkflowTaskDueSoon while SourceJobID is riverJobID(job). Add a small helper near the dispatch path or in notification_definition_helpers.go that sets request.Options.SourceJobKind to DueSoonCheckerArgs{}.Kind(), request.Options.SourceJobID to the checker job ID string, and updates request.Options.CorrelationID to a checker-prefixed value that includes the step execution ID (for example workflow_due_soon_checker:<stepExecutionID>) rather than the workflow_task_due_soon prefix. Use that helper in DueSoonCheckerWorker before notifier.Dispatch. Avoid changing buildWorkflowTaskDueSoonNotificationRequest globally, because regular workflow_task_due_soon River jobs should continue to use worker.JobTypeWorkflowTaskDueSoon metadata. Add or update a focused worker test covering the DueSoonCheckerWorker dispatch metadata.
Implementation guidance: In internal/service/notificationtroubleshooting/service.go, tighten recentProviderJobs so the created_at fallback cannot pull unrelated provider jobs from other notification families. Keep the base query limited to providerKindList(), ordered by id desc and limited to 50. When sourceKinds is non-empty, filter provider jobs with source_job_kind in sourceKinds. If latestSource.ID is non-zero, also include jobs whose source_job_id equals strconv.FormatInt(latestSource.ID, 10) when they have source_job_kind metadata. Only use the created_at >= latestSource.CreatedAt fallback for legacy provider jobs where args/metadata do not contain source_job_kind, and keep that fallback additionally constrained to the same time window. Be careful to read source_job_kind/source_job_id the same way jobListItem does, from args first with metadata fallback if needed, so jobs written with metadata instead of args are not missed. Preserve providerQueueProblems behavior for its 30-day synthetic latestSource by still allowing recent provider jobs for the requested sourceKinds plus legacy metadata-missing rows inside that time window.
