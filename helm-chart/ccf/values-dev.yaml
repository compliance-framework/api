# Development environment overrides
global:
  environment: dev

api:
  replicaCount: 2  # Changed from 1 to 2 for GitOps demo
  image:
    pullPolicy: Never  # For local development

postgresql:
  persistence:
    enabled: false  # No persistence in dev